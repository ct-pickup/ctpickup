/**
 * Bump when `WaiverDocumentBody` copy changes. Users must re-accept before tournament
 * captain flows (claim, payment), pickup availability / final RSVP / field payment,
 * and before submitting guidance requests.
 */
export const CURRENT_WAIVER_VERSION = "v1.1";
